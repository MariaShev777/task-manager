export enum TASK_STATUSES {
  New = 0,
  InProgress = 1,
  Completed = 2,
  Draft = 3,
}

export enum TASK_PRIORITIES {
  Low = 0,
  Middle = 1,
  High = 2,
  Urgent = 3,
  Later = 4,
}

export enum RESPONSE_RESULT {
  SUCCESS = 0,
  FAILURE = 1,
  CAPTCHA = 10,
}
